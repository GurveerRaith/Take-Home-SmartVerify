[pytest]
# The application is imported as `backend.app...`, so the repository root must
# be on the import path. Without this, pytest puts backend/tests on sys.path
# instead and `import backend.app.main` fails.
pythonpath = .

testpaths = backend/tests
